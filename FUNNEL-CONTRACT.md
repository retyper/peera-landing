# 웹 퍼널 → 피어라 앱 연결 규격 (정본)

웹 콘텐츠(이 레포)가 만든 학습 결과를 앱(`dearmydiary`)으로 넘기는 **한 가지 통로**의 규격이다.
보내는 쪽은 여기, 받는 쪽은 `dearmydiary/src/app/lib/vocabImport.ts`. **양쪽이 이 문서만 본다.**

> 이 파일을 고치면 앱 쪽도 같이 고쳐야 한다. 앱은 규격에 어긋나는 링크를 **거부**하지
> 조용히 넘기지 않는다 — 엉뚱한 낱말에 표시가 붙는 게 아무 일도 안 하는 것보다 나쁘기 때문이다.

## 1. 링크 한 줄

```
https://dearmydiary.vercel.app/vocab-boost?known=<b64url>&n=<수>&v=<해시>&t=<트랙>&st=<상자>
  + utm_source / utm_medium / utm_campaign
```

| 파라미터 | 필수 | 뜻 |
|---|---|---|
| `known` | ✅ | `n`비트를 base64url로. 비트 1 = **아는 낱말**(부모가 눌러 표시한 것) |
| `n` | ✅ | 낱말 수. 앱의 트랙 낱말 수와 다르면 **거부** |
| `v` | ✅ | 규약 순서 목록의 FNV-1a 32비트(16진 8자리). 앱 목록과 다르면 **거부** |
| `t` | ⬜ | `en`(교육부 초등 영단어 800) \| `ko`(우리말 기초 어휘). **없으면 `en`** |
| `st` | ⬜ | `n`자리 문자열, 각 자리 `0`~`5` = Leitner 상자. 복습 페이지가 쌓은 진도 |

`utm_*`은 앱이 지우지 않고 남긴다(계측이 읽는다). `known`·`n`·`v`·`t`·`st`는 적용 후 URL에서 지운다.

## 2. 순서 규약 — 양쪽이 똑같이 지켜야 한다

낱말을 **소문자로 바꿔 코드포인트 순 단순 비교**로 정렬한 순서. `localeCompare` 금지.
`v`는 그 순서대로 소문자 낱말을 `\n`으로 이어 붙인 문자열의 FNV-1a 32비트다.

앱 구현: `vocabImport.ts`의 `trackOrder(track)` · `fnv1a()`.
웹 구현: `check.html` 하단 `bApp` 클릭 핸들러.

**현재 값 (2026-09-12 라이브 실측)** — 이 값이 달라졌으면 한쪽 낱말이 바뀐 것이다.

| 트랙 | `n` | `v` |
|---|---|---|
| `en` (교육부 초등 800) | 800 | `f23e916d` |
| `ko` (우리말 기초 어휘) | 569 | `cd86f321` |

`en` 값은 라이브 `check.html`이 실제로 만든 링크에서 읽었고 앱이 거부 없이 받았다
(`dearmydiary/qa/funnelE2E.mjs` 21/21). `ko`는 양쪽이 독립적으로 계산해 일치를 확인했다.

## 3. `st` — 상자 상태 (선택)

웹 복습 페이지(`review.html`)가 쌓은 Leitner 상자를 그대로 넘긴다.
**간격표가 양쪽 같다** — 상자 번호가 1:1로 통하는 건 그 때문이다.

```
상자      0     1     2     3     4     5
다음복습  지금  1일   3일   7일   16일  35일
```

앱: `dearmydiary/src/app/data/vocabDataset.ts`의 `SRS_INTERVAL_DAYS`.
웹: `review.html`. **한쪽만 바꾸면 같은 상자가 두 곳에서 다른 뜻이 된다.**

적용 규칙(앱):
- `known` 비트 1 → **익힘**(상자 5, 35일 뒤). `st`와 무관하게 이게 이긴다.
- `known` 비트 0 + `st` 상자 1 이상 → **그 상자를 물려받는다.** 웹에서 2주 외운 낱말이 앱에서 또 '처음 만나는 낱말'로 나오지 않게.
- `known` 비트 0 + `st` 없음/0 → 지금 복습 대상.

🟥 **`st`는 있으면 좋은 것이다.** 길이가 `n`과 다르거나 `0`~`5` 밖 문자가 있으면 앱은
**조용히 무시하고 `known`만 적용**한다. 복습 진도 때문에 체크 결과 자체가 안 들어가는 일은 없다.

## 4. 새 트랙을 붙일 때 (예: 우리말 낱말 체크 페이지)

앱은 이미 `t=ko`를 받는다. **앱 코드를 고칠 필요가 없다.** 웹이 할 일은 둘뿐이다.

1. 앱의 `ko` 트랙 낱말 전체를 §2 규약대로 정렬해 그 순서로 비트를 만든다.
2. `n`과 `v`를 그 순서로 계산해 같이 보낸다.

`n`·`v`가 맞으면 들어가고, 어긋나면 앱이 거부하며 화면에 안내만 띄운다(기록 무사).
**모르는 `t` 값은 거부한다** — 새 트랙을 만들려면 앱에 트랙을 먼저 추가해야 한다.

🟩 **`ko` 순서는 이미 뽑아 뒀다** → `data/앱-ko트랙-순서.json`
그 파일의 `낱말` 배열 **인덱스가 곧 `known` 비트 순서**다. 같이 든 값도 그대로 쓰면 된다.

```
t=ko   n=569   v=cd86f321
```

앱에서 다시 뽑는 법(앱 낱말이 늘면 `n`·`v`가 바뀌므로 그때 다시 받는다):
`dearmydiary`에서 `trackOrder("ko")` · `fnv1a()`를 돌린다(`lib/vocabImport.ts`).
🟥 **앱 낱말이 늘면 옛 링크의 `v`가 어긋나 거부된다** — 그게 설계대로다(엉뚱한 낱말에
표시가 붙는 것보다 낫다). 앱 낱말을 늘릴 땐 웹에도 새 `n`·`v`를 알려야 한다.

### 🟥 중·고등 = **보류** (사장님 2026-09-12 지시) — 앱에도 없다

답글에 "중등은 없나요"가 붙었다. 원자료는 **이미 이 레포에 있다** —
`data/영어기본어휘-3000-교육부-별표3.json` (초등 800 · **중고 1,200** · 기타 1,000).
그래서 **웹 전용 중등 체크 페이지는 앱 없이 오늘도 만들 수 있다.**

🟥 **다만 그 결과를 앱으로 보내면 안 된다.** 앱 영어 트랙은 1,032개(교육부 초등 800 + 232)뿐이라
중등을 못 덮는다. 중등 링크를 보내면 `n` 불일치로 **거부**된다(기록은 무사하지만 사용자는 막힌다).

| | 낱말 수 | 앱에 있나 |
|---|---|---|
| 교육부 초등 800 | 800 | ⭕ `t=en` |
| 우리말 기초 어휘 | 569 | ⭕ `t=ko` (§4) |
| **교과서 낱말 초1~초6** | **7,706** | 🟨 `t=ko-elem-1` … `t=ko-elem-6` — **아래 §4-A** |
| 교육부 중·고 1,200 | 1,200 | ❌ **없다** |

## 4-A. `t=`는 트랙만이 아니라 **세트**도 받는다 (2026-09-19)

`t=`에 들어갈 수 있는 값이 `en`·`ko` 둘뿐이었다. 그래서 교과서 낱말을 보내면
`vocabImport.ts`가 `null`을 돌려주고 **링크가 통째로 거부**됐다.
맥북 세션이 여섯 세트를 받게 고쳤다(`08dc9e5` · `lib/vocabImport.ts` `resolveTarget`, `data/koElemSets.ts`).

| 세트 | 학년 | `n` | `v` |
|---|---|---|---|
| `ko-elem-1` | 초1 | 874 | `8d910ed9` |
| `ko-elem-2` | 초2 | 559 | `4fdf4e89` |
| `ko-elem-3` | 초3 | 1,332 | `d186050e` |
| `ko-elem-4` | 초4 | 1,077 | `8c2a2f4b` |
| `ko-elem-5` | 초5 | 1,987 | `e09a8ec2` |
| `ko-elem-6` | 초6 | 1,877 | `50a11488` |

- `v`는 `order`를 **소문자로 바꿔 줄바꿈으로 이어 붙인 문자열**의 FNV-1a 32비트다(§2와 같은 방식).
  🟥 **바이트가 아니라 `charCodeAt`(UTF-16)으로 돌린다.** 영어는 두 방식이 같은 값을 내고 **한글만 조용히 달라진다.**
- 🟥 **`v`가 묶어 두는 것은 '차례'가 아니라 낱말의 집합이다.** 웹도 앱도 보내기 직전에
  **소문자 코드포인트 순으로 다시 정렬**한다(`korean4.html` 1548~1549행 — 내가 직접 열어 확인했다).
  그래서 **HTML 안에 `data-w`가 어떤 차례로 나열돼 있든 결과가 같다.**
  낱말을 **하나라도 바꾸거나 더하거나 빼면** `v`가 통째로 바뀐다. 등급순 같은 **다른 정렬 규칙**을 쓰면 역시 다른 값이 나온다.
  ⚠️ 원래 이 줄은 *"순서는 페이지가 보내는 `data-w` 차례다"* 였는데 **두 방향으로 오해를 부른다** —
  ①HTML 차례를 건드리면 깨진다(→ 안 깨진다) ②HTML 차례만 안 건드리면 안전하다(→ 낱말이 바뀌면 깨진다).
  맥북 세션이 `data-w` 두 개를 맞바꿔 보고 **초록이 나와서** 잡아냈다(`qa/webSetParityTest.mjs`).
- 세트를 **겹쳐 받아도** 깨지지 않는다. 먼저 받은 기록의 상자 단계가 유지된다.
- 앱은 **받은 세트 밖의 낱말을 섞지 않는다.** 초4만 가져온 아이에게 초6 낱말을 묻지 않는다.

🟩 **웹도 보낸다 — 2026-09-19 라이브. 순서를 지켰다(앱 배포 확인 → 그다음 웹).**
`peera-landing 5d1137e` · 여섯 페이지 전부 `known·n·v·t=ko-elem-N`을 싣는다.

**라이브↔라이브로 왕복을 봤다**(`peera-growth` `데이터/교과서어휘/검사-한글-실물걷기.mjs` `[5]`):
초4에서 낱말 넷을 진짜 브라우저로 누르고 버튼을 눌러 나간 주소를 붙잡아 `known` 비트를 풀었더니
누른 낱말 넷과 같았고(나른하다·나름·나무라다·나무하다), 그 주소를 라이브 앱에 넣으니
**"복습할 낱말 1073개 · 아는 낱말 4개"** 가 떴다.

🟥 **거부 경로도 확인했다 — 대조는 장식이 아니다.** `v`·`n`·`t`를 각각 한 글자 망가뜨린 링크 셋은
전부 화면에 **"읽지 못했어요"** 가 뜨고 `list_mismatch`·`count_mismatch`·`track_unsupported`로
떨어졌으며 `localStorage`에 아무것도 남지 않았다.

**앱에 중등을 넣는 비용은 코드가 아니라 콘텐츠다.** 별표 3은 낱말만 싣고 뜻을 싣지 않는다.
초등 800의 뜻·예문은 피어라가 직접 썼고(`dearmydiary/src/app/data/englishVocabMoe.ts`),
중등 1,200도 같은 분량의 집필이 필요하다.

🟥 **사장님 지시: 지금은 보류한다.** 웹 전용 페이지도 만들지 않는다. 답글 하나가 근거의 전부다.
다시 꺼낼 때의 순서만 적어 둔다 — 웹 전용으로 수요를 먼저 재고(앱 링크 없이, 나가는 곳은
체험단 폼이나 초등 체크 페이지), 실제로 불리면 그때 앱에 트랙을 연다. 규격은 안 바뀐다(`t=` 값만 하나 는다).

## 4-B. 여러 세트를 한 링크로 — 🟥 **규격만 있고 아직 구현 안 됐다** (2026-09-19)

**왜 여기 적어 두나.** 복습 페이지(`korean-review.html`)는 여섯 학년을 **섞은** 목록이라
대응하는 세트가 없다. 그래서 지금 앱으로 안 보낸다 — 억지로 `t=ko-elem-4`를 붙이면
개수가 안 맞아 `count_mismatch`로 떨어진다. 만들 수는 있고 작다(앱 쪽 30줄 안쪽).
**두 세션이 나중에 각자 다른 규격을 상상하는 것보다, 안 만든 규격이라도 한 곳에 적어 두는 편이 낫다.**

```
t = ko-elem-3,ko-elem-4,ko-elem-6      ← 쉼표로 여러 세트
n = 세 세트 낱말 수의 합
v = 세 세트의 order 를 **세트 id 오름차순으로 이어 붙인** 뒤
    소문자 '\n' 조인의 FNV-1a (§2·§4-A와 같은 방식)
비트 차례 = 같은 이어붙임 순서 (ko-elem-3 전부 → ko-elem-4 전부 → ko-elem-6 전부)
```

🟥 **세트 id 오름차순으로 못 박는 이유**: 복습 페이지가 학년을 어떤 차례로 그리든 링크가 같아야 한다.
그리는 차례에 해시가 따라가면 §4-A에서 이미 한 번 당한 그 사고가 다시 난다.

**누가 언제 만드나 — 아직 아니다.** 이건 새 기능이고 오늘 사장님 승인은 `08dc9e5` 배포까지였다.
그리고 순서상 먼저 잴 것이 있다: **복습 페이지까지 오는 사람이 실제로 있는가.**
9/11 기준 앱 도착이 3명이었으니 여섯 학년을 끝낸 사람은 아직 없을 가능성이 높다.
⇒ **첫 사용자가 실제로 거기 닿는 것을 본 뒤에 만든다.** 사장님이 먼저 하라고 하시면 바로 한다.

## 5. 로그인 — 🟥 옛 제약이 사라졌다 (2026-09-12)

예전엔 이 링크로 들어가면 **역할 선택 → 로그인 → 이름 온보딩** 세 화면을 지나야 했다.
그래서 웹 페이지들이 앱 직행 대신 체험단 폼으로 우회하고 있었다.

**지금은 보호자 동의 한 번이면 바로 말의 정원이 열린다**(계정 없음·세션 안 만듦).
`/vocab-boost`·`/vocab-list`·`/licenses` 세 경로만 열리고 나머지는 정상 온보딩으로 간다.
진도는 그 기기에만 쌓이며, 나중에 계정을 만들면 **그대로 계정으로 합류**한다(유실 없음).

⇒ 웹에서 `로그인 필요` 같은 단서를 붙이지 않는다. 붙일 단서는 하나다: **이 기기에만 저장된다.**

🟥 **2026-09-19 — 벽이 없는데 우리가 벽 앞으로 보내고 있었다.**
체험이 열리는 조건은 셋이다(`vocabTrial.ts` `shouldEnterTrial()`): `isTrialPath()`(= 위 세 경로) **＋** `needsRoleSetup()`
**＋ 주소에 `known=`이 실려 있거나, 이미 체험 표시(`isTrialActive()`)가 있을 것.**
(세 번째의 뒷가지 덕에 **한 번 들어온 사람은 다음 날 주소만 쳐도 이어진다.** 첫 방문에만 `known=`이 필요하다.) 그런데 `koreanN.html`의 앱 버튼은
`dearmydiary.vercel.app/?utm_source=koreanN` — **앱 홈**으로 간다. 홈은 체험 경로가 아니라 **온보딩이 뜬다.**
9/11에 앱까지 온 3명이 3명 다 멈춘 것이 이 모양이다. **고칠 곳은 앱이 아니라 이 규격을 지키는 웹이었다.**
⇒ 앱으로 보낼 때는 **반드시 `/vocab-boost?known=…`** 형태로 보낸다. 홈으로 보내면 벽이 생긴다.

앱 구현: `dearmydiary/src/app/lib/vocabTrial.ts`,
검증: `qa/vocabTrialTest.mjs` · `qa/vocabTrialE2E.mjs`(`BASE=`로 배포본 겨냥 가능).

## 6. 저장 키 — 겹치지 않게

| 키 | 주인 |
|---|---|
| `check800_v1` · `check800_done_v1` · `check800_last_v1` · `check800_mode_v1` | `check.html` |
| `review800_v1` · `review800_meta_v1` | `review.html` |
| `vocabBoostRecords` · `vocabImport_v1` · `vocabTrialMode_v1` | 앱 |

웹과 앱은 **다른 오리진**이라 실제로 섞이진 않는다. 새 페이지를 만들 때 웹 안에서만 안 겹치면 된다.

## 7. 바꿀 때 순서

1. 이 문서를 먼저 고친다.
2. 앱(받는 쪽)을 **먼저** 배포한다 — 관대하게 받도록. 모르는 값은 무시하거나 안전하게 거부.
3. 그 다음 웹(보내는 쪽)을 배포한다.

거꾸로 하면 옛 앱을 쓰는 사람의 링크가 거부된다.
